require 'spec_helper'

describe Mina::Whenever do
  it 'has a version number' do
    expect(Mina::Whenever::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
